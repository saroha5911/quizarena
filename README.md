# quizarena
QuizArena is a ready-to-launch React + TypeScript app template built for interactive quizzes and skill testing. It features full state management via React Context, custom quiz configurations (difficulty, timer, question count), a seed-based Daily Challenge mode, a local leaderboard, Web Audio API sound effects, and dark/light mode.
